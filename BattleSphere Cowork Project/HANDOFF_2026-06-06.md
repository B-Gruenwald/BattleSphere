# BattleSphere – Session Handoff (2026-06-06)

## Project overview
BattleSphere is a Next.js (App Router) web app for running map-based Warhammer 40K campaigns. Stack: React 19, Supabase (Postgres + RLS + Storage). Live on Vercel at https://battle-sphere-topaz.vercel.app. GitHub: https://github.com/B-Gruenwald/BattleSphere.

## File access — do this first
The source code is NOT in the default workspace folder. At the start of every session you must request access to the source directory:

```
request_cowork_directory: ~/Desktop/BattleSphere
```

This mounts to `/sessions/<session-id>/mnt/BattleSphere`. All code edits go here.
The "BattleSphere Cowork Project" folder (SQL migrations, handoffs) is a separate mount — already available at `/sessions/<session-id>/mnt/BattleSphere Cowork Project`.

## Autonomous git push — how to do it
```bash
cd /sessions/<session-id>/mnt/BattleSphere
git add <files>
git commit -m "..."
git push
```
If commit fails with `HEAD.lock` error: call `mcp__cowork__allow_cowork_file_delete` on the lock path, then `rm -f .git/HEAD.lock`, then retry.
Vercel auto-deploys on every push to `main`.

## Last known commit
`67c42a1` — `feat: sub-territory influence bleed — event-gated, replaces hardcoded ×0.5`

---

## Key technical rules
- All files use `.js` — never `.tsx` or TypeScript
- `'use client'` required for any component with event handlers or useState
- Server components (`page.js`) use `createClient` from `@/lib/supabase/server`
- Client components use `createClient` from `@/lib/supabase/client`
- Admin operations (cross-user data) use `createAdminClient` from `@/lib/supabase/admin`
- Benjamin is non-technical — keep explanations clear and jargon-free

---

## Completed features (cumulative)

### Core platform
- Map-based narrative campaign system with territories, factions, battles, influence
- Influence modes: `standard` (Win +3/+1, Draw +2), `victory` (Win +1 only), `off` (manual)
- Territory cascade (event-gated): winning faction gains influence in connected territories
- XP system: `app/lib/xp.js` — `calcPlayerXP()` / `getXPRank()`, ranks Recruit → Legend
- League format campaigns (W/D/L points table instead of influence map)
- Crusade unit records (XP, kills, upgrades, scars per unit per campaign)
- Army portfolios with unit portraits and photo galleries
- Player profiles, standings, achievements

### Admin / organiser tools
- Admin page (`/c/[slug]/admin`) — General Settings + Campaign Digest only
- Join requests: inline on `/c/[slug]/players` via `InlineJoinRequests.js`
- Remove player: inline on players page via `RemovePlayerButton.js`
- Delete battle: inline on battles page via `DeleteBattleButton.js` (reverses influence + cascade)
- Manage Factions: `AdminFactionManager.js` in admin section

### Notifications
- In-app bell + inbox (`/inbox`) via `user_notifications` table
- Triggered on: battle logged (opponent notified), campaign joined, achievement awarded, campaign event goes live
- Email digest via `apicron/campaign-digest/route.js` (Resend)

### SEO
- `metadataBase` in `layout.js`
- Canonical URLs on all 7 public pages (`/`, `/campaigns`, `/armies`, `/campaign/[slug]`, `/armies/[id]`, `/units/[id]`, `/c/[slug]/battle/[id]`)
- Battle report OG image (`opengraph-image.js`): full-bleed photo, subject at x=200 (shifted left -200), gradient overlay, text zone right 265px; uses portrait-flagged photo first

### Factions (added this session)
- `factions` table now has `description TEXT` and `image_url TEXT` columns
- `AdminFactionManager.js`: each row has **▸ Edit Details** gold button expanding a two-column panel with description textarea + cover image upload (bucket: `faction-images`, public)
- Faction detail page (`/c/[slug]/faction/[id]/page.js`): cover image hero → description block (coloured left border) → faction name/header → record strip
- Campaign dashboard faction standings: shows 40×40 thumbnail (coloured border) + description snippet per faction row

### Sub-territory influence bleed (added this session)
Previously hardcoded: battles in sub-territories counted at ×0.5 toward parent territory influence (display only, never written to DB). Now event-gated:
- `campaign_events.sub_territory_bleed BOOLEAN DEFAULT false` (migration run)
- `EventForm.js`: new toggle section "Sub-territory Influence Bleed" below Territory Cascade
- `CampaignMap.js`: accepts `subTerritoryBleed` prop; ×0.5 aggregation only applies when true
- Campaign dashboard (`/c/[slug]/page.js`): derives `subTerritoryBleed` from active events, passes to CampaignMap
- Territory detail page: queries active bleed event, conditionally applies ×0.5 to influence bar and footnote

### Email / spam fixes (this session)
- Email authentication: SPF (`include:_spf.resend.com`), DKIM (Resend verified), DMARC (`p=none`) all confirmed correct
- Digest subject line: was hardcoded "What's new on BattleSphere" — now uses first announcement title: `${first.title} — BattleSphere` (`app/api/cron/campaign-digest/route.js`)

### Database indexes (this session)
First-ever index migration (`migration_indexes.sql`) — run 2026-06-06. Covers all high-frequency query columns across: `battles`, `territory_influence`, `territories`, `factions`, `campaign_members`, `campaign_events`, `warp_routes`, `achievements`, `army_units`, `army_unit_photos`, `armies`, `crusade_unit_records`, `campaign_army_records`, `battle_event_bonuses`, `battle_cascade_bonuses`, `faction_photos`, `user_notifications`, `profiles`, `campaigns`.

---

## Storage buckets (all public)
| Bucket | Used for |
|--------|----------|
| `territory-images` | Territory cover images |
| `faction-images` | Faction cover images |
| `battle-photos` | Battle report photos |
| `army-photos` | Army / unit portraits |

---

## Pending / known issues
- Mobile responsiveness deferred
- `campaign_events.sub_territory_bleed` migration must be run before the new Event Form toggle saves correctly (migration file: `migration_sub_territory_bleed.sql`)
- Faction cover images require `faction-images` bucket to exist in Supabase Storage (public, with RLS policies from `migration_faction_images_storage_rls.sql`)

---

## Key files reference
| File | Purpose |
|------|---------|
| `app/lib/influence.js` | All influence helpers: apply/reverse base, event bonuses, territory cascade |
| `app/lib/xp.js` | Player XP calculation and rank labels |
| `app/components/BattleLogForm.js` | Battle logging — calls influence + event + cascade functions |
| `app/components/BattleEditForm.js` | Battle editing — same call chain |
| `app/components/EventForm.js` | Campaign event creation/editing (shared for new + edit) |
| `app/components/CampaignMap.js` | SVG map renderer — accepts `subTerritoryBleed` prop |
| `app/components/AdminFactionManager.js` | Faction CRUD + description/image editor |
| `app/components/DeleteBattleButton.js` | Inline battle delete with influence reversal |
| `app/components/InlineJoinRequests.js` | Inline join request approve/reject |
| `app/components/RemovePlayerButton.js` | Inline player removal |
| `app/api/cron/campaign-digest/route.js` | Email digest cron — subject now uses announcement title |
| `app/c/[slug]/page.js` | Campaign dashboard — derives `subTerritoryBleed` from active events |
| `app/c/[slug]/territory/[id]/page.js` | Territory detail — queries bleed event, conditional ×0.5 |
| `app/c/[slug]/faction/[id]/page.js` | Faction detail — cover image + description in header |
